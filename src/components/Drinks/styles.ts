import styled from 'styled-components'

export const DrinksList = styled.ul`
    display: flex;
    flex-wrap: wrap;
    justify-content: center;  
    margin: 0 auto;
    height: 78vh;
    padding: 1rem;
`